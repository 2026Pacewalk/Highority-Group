// Base path for the admin dashboard. Kept off the obvious "/admin" so
// automated scanners and bots don't find the login page. To change it,
// edit this one value (and restart/redeploy).
export const ADMIN_BASE = '/hg-control';
